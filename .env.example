# Payload CMS Secret (generate with: openssl rand -hex 32)
PAYLOAD_SECRET=

# MongoDB Database URL
# Local MongoDB: mongodb://localhost:27017/matt3am
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/matt3am?retryWrites=true&w=majority
DATABASE_URL=mongodb://localhost:27017/matt3am

# API Configuration for Client-Side
# This variable is prefixed with NEXT_PUBLIC_ so it's available in the browser
# Base URL for API requests (default: /api for same-origin requests)
# For external API, use full URL: https://api.example.com
NEXT_PUBLIC_API_URL=/api
