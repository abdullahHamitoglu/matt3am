# 🛒 تحديث: نظام السلة والطلبات

## ✨ المميزات الجديدة

تم إضافة نظام شامل ومتكامل لإدارة السلة والطلبات يتضمن:

### 🛍️ نظام السلة (Shopping Cart)
- إضافة، حذف، وتعديل المنتجات
- دعم المستخدمين الضيوف (Guest Checkout)
- حساب تلقائي للأسعار والضرائب
- تخصيصات وملاحظات خاصة لكل منتج
- دعم كوبونات الخصم
- انتهاء صلاحية السلة بعد 24 ساعة للضيوف

### 📦 نظام الطلبات (Orders)
- إنشاء طلب من السلة
- ثلاثة أنواع طلبات: تناول في المطعم، سفري، توصيل
- مراحل متعددة للطلب (قيد الانتظار → تحضير → جاهز → مكتمل)
- تتبع حالة كل منتج في المطبخ
- معلومات العميل وعنوان التوصيل
- طرق دفع متعددة
- رقم طلب فريد تلقائي

### 🎨 مكونات React جاهزة
- **CartIcon**: أيقونة السلة مع عداد المنتجات
- **ShoppingCart**: عرض السلة الكاملة
- **AddToCartButton**: زر إضافة للسلة
- **CheckoutPage**: صفحة الدفع الكاملة
- **OrderTracking**: تتبع حالة الطلب

## 📁 الملفات الجديدة

```
src/
├── collections/
│   └── Cart.ts                     ✅ جديد
├── services/
│   └── cart.service.ts             ✅ جديد
├── hooks/
│   └── cart/
│       ├── index.ts                ✅ جديد
│       ├── useCart.ts              ✅ جديد
│       └── useCheckout.ts          ✅ جديد
└── components/
    ├── cart/
    │   ├── index.ts                ✅ جديد
    │   ├── ShoppingCart.tsx        ✅ جديد
    │   ├── CheckoutPage.tsx        ✅ جديد
    │   ├── AddToCartButton.tsx     ✅ جديد
    │   └── CartIcon.tsx            ✅ جديد
    └── orders/
        ├── index.ts                ✅ جديد
        └── OrderTracking.tsx       ✅ جديد

docs/
├── CART_AND_ORDERS_SYSTEM.md       ✅ التوثيق الكامل
└── QUICK_START_CART.md             ✅ دليل البداية السريعة
```

## 🚀 الاستخدام السريع

### 1. إضافة أيقونة السلة

```tsx
import { CartIcon } from '@/components/cart'

<CartIcon restaurantId="your-restaurant-id" />
```

### 2. زر إضافة للسلة

```tsx
import { AddToCartButton } from '@/components/cart'

<AddToCartButton
  restaurantId="restaurant-id"
  menuItem={{
    id: 'item-id',
    name: 'برجر لحم',
    price: 35
  }}
/>
```

### 3. صفحة السلة

```tsx
import { ShoppingCart } from '@/components/cart'

<ShoppingCart restaurantId="restaurant-id" />
```

### 4. صفحة الدفع

```tsx
import { CheckoutPage } from '@/components/cart'

<CheckoutPage restaurantId="restaurant-id" />
```

### 5. تتبع الطلب

```tsx
import { OrderTracking } from '@/components/orders'

<OrderTracking orderId="order-id" />
```

## 🔧 API Hooks

### useCart Hook

```tsx
const {
  cart,
  loading,
  itemCount,
  total,
  addToCart,
  removeItem,
  updateQuantity,
  clearCart,
} = useCart({ restaurantId: 'your-restaurant-id' })
```

### useCheckout Hook

```tsx
const {
  loading,
  error,
  orderId,
  createOrderFromCart,
} = useCheckout()

await createOrderFromCart({
  cartId: cart.id,
  customer: {
    name: 'أحمد محمد',
    phone: '0501234567'
  },
  orderType: 'delivery',
  // ... باقي البيانات
})
```

## 📖 التوثيق

للمزيد من التفاصيل:
- [📚 التوثيق الكامل](./docs/CART_AND_ORDERS_SYSTEM.md)
- [⚡ دليل البداية السريعة](./docs/QUICK_START_CART.md)

## 💡 ملاحظات مهمة

1. **الضريبة**: معدل الضريبة الافتراضي 15% (قابل للتخصيص)
2. **الجلسات**: يتم إدارة جلسات الضيوف تلقائياً
3. **الصلاحيات**: السلة متاحة للجميع، الطلبات محمية
4. **التحديث التلقائي**: OrderTracking يحدث كل 30 ثانية

## 🎯 مسار العمل الكامل

```
عميل يتصفح القائمة
  ↓
يضيف منتجات للسلة (مع تخصيصات)
  ↓
يراجع السلة ويعدل الكميات
  ↓
ينتقل لصفحة الدفع
  ↓
يدخل بياناته وعنوان التوصيل
  ↓
يؤكد الطلب
  ↓
يتابع حالة الطلب في الوقت الفعلي
  ↓
يستلم الطلب
```

## 🔐 الأمان

- تصفية السلة حسب المستخدم/الجلسة
- صلاحيات محكمة للطلبات
- التحقق من البيانات في الـ Backend
- حسابات تلقائية لمنع التلاعب بالأسعار

---

تم التطوير بواسطة نظام إدارة المطاعم المتكامل 🍽️
